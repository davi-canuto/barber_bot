name: Mark cut

on:
  schedule:
    - cron: '5 0 * * *'

jobs:
  make-http-request:
    runs-on: ubuntu-latest
    steps:
      - name: Make HTTP Request
        run: |
          curl --location --request POST 'https://auto-barber-schedule.vercel.app/api/go'